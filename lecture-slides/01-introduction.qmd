---
title: "Applied Bayesian Econometrics"
subtitle: "1 — Introduction and the Bayesian paradigm"
---

# Welcome!

## Kicking off Applied Bayesian Econometrics

- What is this course about?
- How is this course organized?
- Set up your Python environment
- A first look at the Bayesian paradigm

## What is Bayesian econometrics?

**How should we reason about unknown quantities when the data are limited and noisy?**

The Bayesian answer is to treat unknowns as random and update beliefs with evidence.

- Parameters are not fixed numbers we "estimate"; they are quantities we are *uncertain* about
- We start from a prior, observe data through a likelihood, and obtain a posterior
- Every answer comes with a full distribution, not just a point and a standard error

This is a coherent framework for **quantifying uncertainty**, exactly what economics
and finance need when models are large and data are scarce.

## Why Bayesian methods, and why now?

- Modern datasets are high-dimensional: many predictors, few clean observations
- Shrinkage priors regularise these models in a principled way
- Computation (MCMC) makes once-intractable models routine
- Forecasting, policy analysis, risk, and causal inference all benefit

. . .

The less glamorous reality: most of the work is careful modelling and honest
accounting for uncertainty, not magic.

## Topics of this course

- Subjective probability and the Bayesian paradigm
- MCMC: Gibbs sampling and Metropolis-Hastings
- Shrinkage via Minnesota priors
- Bayesian VARs and state-space models for macroeconomics
- Stochastic volatility and portfolio choice for finance
- Bayesian machine learning for causal inference in microeconomics

By the end you should

- understand the theoretical foundations of Bayesian inference
- analyse high-dimensional data with modern Bayesian techniques
- implement standard algorithms by writing your own Python code

## Objectives

We move from theory to working code in every session.

- Plan, implement, and criticise Bayesian analyses from scratch
- Master the relevant models and apply them in new, complex contexts
- In plain words: *learn Bayesian econometrics by building it in Python*

The course is grounded in the textbook literature and recent papers. Be prepared
to read and discuss; see the [literature list](../literature.qmd).

# Administration

## Team and communication

- **Coordinators:** Prof. Dr. Roxana Halbleib and Dr. Christoph Frey
- **Department:** Statistik und Ökonometrie
- All material, updates, and announcements are posted on **ILIAS**
- The exercise sessions are where the Python work happens — come prepared

## How the course works

- Weekly 2-hour lecture: theory and methods
- Weekly 2-hour exercise session: implement the method in Python
- 6 ECTS, taught in English, recommended for the 3rd semester

. . .

The learning curve is steep, especially around MCMC. Reach out early when
something is unclear rather than waiting.

## Prerequisites

You are expected to bring:

- mathematics, statistics, and probability theory
- regression analysis: estimation and hypothesis testing
- **Python** coding — this is essential for the exercises

If your Python is rusty, refresh it before week 1. We assume comfort with NumPy
and basic scientific computing.

## Assessment

- Written final exam, **90 minutes**
- Permitted: non-programmable calculator, a hard-copy German–English dictionary,
  and a **one-sided A4 hand-written cheat sheet**
- The grade is **100%** the written exam

# The Bayesian paradigm

## Bayes' theorem

For parameters $\theta$ and data $y$:

$$
p(\theta \mid y) \;=\; \frac{p(y \mid \theta)\,p(\theta)}{p(y)}
\;\propto\; \underbrace{p(y \mid \theta)}_{\text{likelihood}}\;\underbrace{p(\theta)}_{\text{prior}}
$$

- The **posterior** $p(\theta \mid y)$ is what we report
- The **prior** $p(\theta)$ encodes what we knew beforehand
- The marginal likelihood $p(y)$ is a normalising constant

. . .

Everything in this course is, in one way or another, a strategy for computing or
approximating that posterior.

## Frequentist vs. Bayesian, in one slide

| | Frequentist | Bayesian |
|---|---|---|
| Parameter | fixed, unknown | random, uncertain |
| Probability | long-run frequency | degree of belief |
| Output | estimate + CI | posterior distribution |
| Uncertainty | sampling variability | full posterior |

Neither is "correct" in the abstract. The Bayesian framing is especially natural
when we have prior information and want coherent uncertainty.

## A first example: estimating a probability

Suppose we observe $y$ successes in $n$ trials and want the success probability $\theta$.

- Likelihood: $y \mid \theta \sim \text{Binomial}(n, \theta)$
- Prior: $\theta \sim \text{Beta}(a, b)$
- Posterior: $\theta \mid y \sim \text{Beta}(a + y,\; b + n - y)$

The Beta prior is **conjugate** to the binomial likelihood, so the posterior is
again Beta. We will lose conjugacy quickly, which is exactly why we need MCMC.

## The same example in Python

```{python}
#| echo: true
#| output-location: column
import numpy as np
from scipy import stats
import matplotlib.pyplot as plt

# data: 8 successes in 12 trials
y, n = 8, 12
a, b = 2, 2            # Beta(2,2) prior

grid = np.linspace(0, 1, 200)
prior = stats.beta(a, b).pdf(grid)
post  = stats.beta(a + y, b + n - y).pdf(grid)

fig, ax = plt.subplots(figsize=(4, 3))
ax.plot(grid, prior, "--", label="prior")
ax.plot(grid, post, label="posterior")
ax.axvline(y / n, color="grey", lw=0.8, label="MLE")
ax.set_xlabel(r"$\theta$"); ax.legend()
plt.tight_layout()
plt.show()
```

The posterior sits between the prior and the data, and it tightens as $n$ grows.

## What you should take away today

- Bayesian inference combines a prior and a likelihood into a posterior
- The posterior *is* the answer: it carries the full uncertainty
- Conjugate models are rare; computation (MCMC) is what makes the rest possible
- We will implement every method in Python, starting next week

## Next steps

- Set up Python and confirm NumPy, SciPy, and Matplotlib run
- Skim Lambert, *A Student's Guide to Bayesian Statistics*, chapters 1–3
- Bring questions to the first exercise session

Material and updates: **ILIAS**. See the [course homepage](../index.qmd) for the
schedule and reading list.
